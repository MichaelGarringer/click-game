import React from "react";
import "./Scores.css";

//pass the image into each card so all 12 are rendered
function Scores(){
    return(
    <h1>Score will be displayed here once I figure it out :)</h1>
    )
}
export default Scores;